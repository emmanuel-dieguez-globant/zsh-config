#compdef activate

_activate() {
  if [ -d $VENV_HOME ]; then
    local -a directories=($VENV_HOME/*)
    directories=("${directories[@]##*/}")

    _describe -t directories 'directories' directories
  fi
}

compdef _activate activate
